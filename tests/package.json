{
  "name": "traderadar-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright smoke tests for TradeRadar (no-build React via Babel standalone).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
